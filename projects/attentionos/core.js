(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.AttentionOS=api;})(typeof self!=='undefined'?self:this,function(){function clamp(n,a,b){return Math.min(b,Math.max(a,n))}function scoreTask(t){const impact=clamp(Number(t.impact)||1,1,5),urgency=clamp(Number(t.urgency)||1,1,5),effort=clamp(Number(t.effort)||1,1,5);return Math.round((impact*2+urgency*1.5+(6-effort))*10)/10}function rankTasks(tasks){return [...tasks].sort((a,b)=>scoreTask(b)-scoreTask(a))}function focusScore(s){const mins=Math.max(0,Number(s.minutes)||0),ints=Math.max(0,Number(s.interruptions)||0),completed=!!s.completed;return clamp(Math.round((Math.min(mins,90)/90*70)+(completed?20:0)-ints*6+10),0,100)}function summarize(sessions){const xs=sessions||[];if(!xs.length)return{sessions:0,minutes:0,avgFocus:null,interruptions:0,bestHour:null};const minutes=xs.reduce((a,s)=>a+(Number(s.minutes)||0),0),interruptions=xs.reduce((a,s)=>a+(Number(s.interruptions)||0),0);const avgFocus=Math.round(xs.reduce((a,s)=>a+focusScore(s),0)/xs.length);const hours={};xs.forEach(s=>{const h=new Date(s.startedAt).getHours();(hours[h]??=[]).push(focusScore(s))});const bestHour=Object.entries(hours).sort((a,b)=>b[1].reduce((x,y)=>x+y,0)/b[1].length-a[1].reduce((x,y)=>x+y,0)/a[1].length)[0]?.[0]??null;return{sessions:xs.length,minutes,avgFocus,interruptions,bestHour}}function recommend(tasks,sessions){const ranked=rankTasks(tasks);const summary=summarize(sessions);const first=ranked[0];if(!first)return'Add one meaningful task, then start a short focus block.';const length=summary.avgFocus!==null&&summary.avgFocus<55?20:summary.avgFocus!==null&&summary.avgFocus>80?45:30;return`Next: ${first.title}. Try ${length} focused minutes, then review.`}return{scoreTask,rankTasks,focusScore,summarize,recommend};});