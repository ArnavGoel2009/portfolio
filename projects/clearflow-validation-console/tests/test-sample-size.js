const assert=require('assert'),S=require('../sample-size');const n=S.requiredN({expectedRate:.7,maxHalfWidth:.1});assert(n>0);assert(S.wilsonWidth(.7,n)/2<=.1);if(n>2)assert(S.wilsonWidth(.7,n-1)/2>.1);const p=S.trialPlan(['auger','brush']);assert.equal(p.total,p.perTool*2);assert(p.perTool>=p.drivers.success&&p.perTool>=p.drivers.jam);console.log(JSON.stringify({status:'PASS',tests:5,n,plan:p},null,2));
