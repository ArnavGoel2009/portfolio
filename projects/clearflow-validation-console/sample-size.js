'use strict';
function wilsonWidth(p,n,z=1.96){if(!(p>=0&&p<=1)||!Number.isInteger(n)||n<1)throw new Error('Invalid inputs');const z2=z*z,d=1+z2/n,h=z*Math.sqrt((p*(1-p)+z2/(4*n))/n)/d;return 2*h}
function requiredN({expectedRate=.7,maxHalfWidth=.1,confidenceZ=1.96,minN=2,maxN=100000}={}){if(!(maxHalfWidth>0&&maxHalfWidth<.5))throw new Error('maxHalfWidth must be between 0 and 0.5');for(let n=minN;n<=maxN;n++)if(wilsonWidth(expectedRate,n,confidenceZ)/2<=maxHalfWidth)return n;throw new Error('No sample size found within maxN')}
function trialPlan(toolHeads,{expectedSuccess=.7,successHalfWidth=.15,expectedJam=.15,jamHalfWidth=.1}={}){const heads=[...new Set((toolHeads||[]).map(String).map(s=>s.trim()).filter(Boolean))];if(!heads.length)throw new Error('At least one tool head is required');const perToolSuccess=requiredN({expectedRate:expectedSuccess,maxHalfWidth:successHalfWidth}),perToolJam=requiredN({expectedRate:expectedJam,maxHalfWidth:jamHalfWidth}),perTool=Math.max(perToolSuccess,perToolJam);return{toolHeads:heads,perTool,total:perTool*heads.length,drivers:{success:perToolSuccess,jam:perToolJam},assumptions:{expectedSuccess,successHalfWidth,expectedJam,jamHalfWidth,confidence:'approximately 95% Wilson interval'},note:'Planning calculation only; independence and representative trial conditions still need experimental control.'}}
module.exports={wilsonWidth,requiredN,trialPlan};
