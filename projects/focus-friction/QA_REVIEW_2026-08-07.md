# QA Review - 7 August 2026

## Bugs found and fixed
- Overnight scheduled windows now continue correctly after midnight based on the previous selected day.
- Focus-minute accounting is capped at the planned session end instead of growing indefinitely if Stop is clicked late.
- Background logic now finalizes expired manual sessions and records their bounded minutes.
- Scheduled focus mode now shows an ON badge even when there is no manual countdown.

## Tests
The expanded core reviewer suite covers the overnight after-midnight boundary, bounded session accounting, domain matching, temporary unlock expiry, configuration sanitization, vocabulary/intention validation and DNR rule construction. The reviewer suite passed before these fixes were committed.

## Still unverified
- Manual unpacked-extension integration in real Chrome.
- Redirect behavior and temporary unlock flow under an actual browser session.
- Visual polish in the actual Chrome extension popup/options rendering.
- The redirect currently returns the user to the domain root after unlock rather than preserving the exact original path.

## Release status
REVIEW FIRST. Real MVP source exists and core logic is tested, but production-ready is not yet claimed.
